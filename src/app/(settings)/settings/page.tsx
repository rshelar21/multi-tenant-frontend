import React from 'react';
import { UserSettings } from '@/containers/settings';

const SettingsPage = () => {
  return (
    <div>
      <UserSettings />
    </div>
  );
};

export default SettingsPage;
